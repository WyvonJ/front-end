/**
 * 插入排序
 * @export
 * @class Insertion
 */
export default class Insertion {
  /**
   * 排序方法
   * @static
   * @param {Array<number>} arr
   * @return {Array<number>}
   * @memberof Insertion
   */
  static run(arr) {
    // TODO
    return arr;
  }
}
