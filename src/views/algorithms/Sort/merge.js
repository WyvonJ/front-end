/**
 * 归并排序
 * @export
 * @class Merge
 */
export default class Merge {
  /**
   * 排序方法
   * @static
   * @param {Array<number>} arr
   * @return {Array<number>}
   * @memberof Merge
   */
  static run(arr) {
    return arr;
  }
}
